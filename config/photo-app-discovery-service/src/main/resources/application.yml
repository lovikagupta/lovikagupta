eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
server:
  port: 8761
spring:
  application:
    name: service-discovery
  boot:
    admin:
      client:
        url: http://localhost:8763