server:
  port: 8020
spring:
  application:
    name: school-service
  cloud:
    consul:
      discovery:
        register: true
        service-name: student-service
      host: localhost
      port: 8500